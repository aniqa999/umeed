import 'package:flutter/material.dart';

class CardData {
  final IconData icon;
  final String label;
  final Color color;
  final String route;
  const CardData(this.icon, this.label, this.color, this.route);
}